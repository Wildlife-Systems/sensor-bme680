# Build artifacts
sensor-bme680
*.o
*.deb

# Cygwin and MinGW gcc append .exe, which a bare binary name does not match
*.exe

# Debian build directories
debian/sensor-bme680/
debian/.debhelper/
debian/files
debian/debhelper-build-stamp
debian/*.substvars
debian/*.log
