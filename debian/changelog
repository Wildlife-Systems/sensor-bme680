sensor-bme680 (2.3.0) stable; urgency=medium

  * Two config entries without a sensor_id no longer get the same one. Both
    used to be "<serial>_bme680", so a sensor at each I2C address was merged
    downstream into one series without a word. A lone entry keeps that id, so
    no existing node's datastreams move; where more than one entry omits it,
    each is "<serial>_bme680_0x76" or "_0x77". Assigned by the library at
    load time rather than per read, and it warns about any two entries that
    still share an id
  * Call ws_log_init, so the library's warnings reach syslog under this
    driver's name; they were going out under no name at all
  * Mock readings declare location "{{node}}", as sensor-onboard's mock
    declares its physical sensors, so every driver's mock has one shape.
    They used to emit location null, which is what an unconfigured sensor
    reports, and so disagreed with the shell driver's mock
  * Fail with nothing printed, exit 20, when sc-prototype is unavailable. The
    template was checked per reading, after the sensor had been read, and the
    unbuilt reading was appended anyway: the output was "[,,,]" with exit 0,
    which is not JSON. The check now happens once, before the I2C bus is
    opened, via the library's ws_require_prototype()
  * Out of memory while building the array is also exit 20 with no output,
    rather than exit 0 with no output, which read as success to sr
  * sensor_id and sensor_name are now escaped, by libwildlifesystems 1.3.0. A
    quote in either, typed into the config file, ended the JSON string early
    and invalidated every reading this driver emitted
  * Accept the measurements that list advertises. "sensor-bme680 temperature"
    and the other three were rejected with exit 20, though list named all four
    and sensor-dht11 accepted its own two, so anything discovering a driver's
    capabilities through list could ask for a measurement and be refused
  * A reading whose sensor_id cannot be determined is emitted with
    "sensor_id":null rather than dropped. On a host with no Serial line in
    /proc/cpuinfo, which a non-Pi I2C host may be, every reading was being
    discarded and the node reported as having no sensors at all
  * The usage line is generated from the measurement list rather than written by
    hand, so it cannot omit what the driver accepts
  * "mock" is a table of values passed to the library rather than hand-built
    JSON, so it cannot drift from the real output format
  * "enable" now enables I2C, by adding dtparam=i2c_arm=on to the Pi boot
    config, instead of printing a note telling the user to run raspi-config.
    The kernel exposes no I2C bus until that is set, so a driver whose "enable"
    did nothing left the sensor unusable by its own instructions. Running it
    twice is safe: the second run reports that it is already enabled
  * Build the output array with the library's JSON array builder instead of
    realloc and strcat, which was repeated once per measurement. The four
    near-identical blocks are now one call each
  * Use the library's WS_UNIT_* constants rather than unit string literals
  * Config parsing moves to the library's ws_config_iter_* and the common
    fields to an embedded ws_sensor_config_base_t, leaving i2c_addr as the only
    field this driver declares and parses for itself
  * No longer crashes when no sensor_id can be determined. A config entry
    without sensor_id on a host whose /proc/cpuinfo has no Serial line (a
    non-Pi I2C host) left sensor_id NULL, which was then passed to strlen().
    The reading is now emitted with a null sensor_id instead
  * The four per-measurement id allocations are no longer unchecked, and the
    duplicated size arithmetic is now in one helper
  * "mock" no longer prints "(null)_temperature" where the host has no Pi
    serial; it falls back to a fixed id, mock being the command meant to work
    without the hardware
  * Rebuild against libwildlifesystems 1.3.0, so "{{node}}" in a sensor's
    location resolves to the node's own coordinates from /etc/geolocation
    instead of being emitted as an unresolved token
  * Require libwildlifesystems-dev (>= 1.3.0). The library is static, so the
    behaviour above is fixed at build time: a binary built against 1.2.0 would
    satisfy the old pin and silently emit unresolved tokens under the same
    package version
  * Rename the gas reading's "sensor" from "bme680_gas" to
    "bme680_gas_resistance", matching the "_gas_resistance" its sensor_id has
    always used. The two disagreed, so the same reading answered to one name in
    "sensor" and another in "sensor_id". The sensor_id is the identifier already
    in use downstream, so "sensor" is the side that moves. Note that
    "sr --sensor bme680_gas" becomes "sr --sensor bme680_gas_resistance"; the
    measurement name used by "sensor-bme680 gas" is unchanged

 -- Ed Baker <ed@ebaker.me.uk>  Sat, 12 Sep 2026 14:00:00 +0100

sensor-bme680 (2.2.1) stable; urgency=medium

  * Zero-initialise default_config. It sets every field explicitly except
    location, which was left reading uninitialised stack memory on nodes with
    no config file; a garbage source of WS_LOC_EXPLICIT would have emitted a
    GeoJSON Point built from uninitialised coordinates

 -- Ed Baker <ed@ebaker.me.uk>  Sat, 12 Sep 2026 10:00:00 +0100

sensor-bme680 (2.2.0) stable; urgency=medium

  * Report device "bme680" and the sensor's configured location
  * Support "location" in /etc/ws/sensors/bme680.json
  * Use ws_json_object_end for config object bounds, so a config entry may
    contain nested objects
  * Rebuild against libwildlifesystems 1.2.0
  * Declare libwildlifesystems-dev (>= 1.2.0) in Build-Depends and
    sensor-control (>= 1.3.0) in Depends. Neither was declared at all,
    though the binary links -lwildlifesystems and runs sc-prototype

 -- Ed Baker <ed@ebaker.me.uk>  Fri, 11 Sep 2026 16:00:00 +0100

sensor-bme680 (2.1.0) stable; urgency=medium

  * Fix humidity and gas calculations

 -- Ed Baker <ed@ebaker.me.uk>  Tue, 10 Feb 2026 14:00:00 +0000

sensor-bme680 (2.0.3) stable; urgency=medium

  * Rebuild against libwildlifesystems 1.1.1

 -- Ed Baker <ed@ebaker.me.uk>  Tue, 27 Jan 2026 14:00:00 +0000

sensor-bme680 (2.0.1) stable; urgency=medium

  * Refactor to use libwildlifesystems shared library
  * Add setup, enable, mock commands for consistent CLI
  * Update man page with all commands
  * Fix addr_str buffer size

 -- Ed Baker <ed@ebaker.me.uk>  Tue, 27 Jan 2026 12:00:00 +0000

sensor-bme680 (2.0.0) stable; urgency=medium

  * Rewrite in C

 -- Ed Baker <ed@ebaker.me.uk>  Sun, 25 Jan 2026 12:00:00 +0000
